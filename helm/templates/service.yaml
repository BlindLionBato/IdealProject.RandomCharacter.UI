apiVersion: v1
kind: Service
metadata:
  name: {{ include "random-character-ui.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "random-character-ui.fullname" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}